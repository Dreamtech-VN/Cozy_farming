{
  "name": "sunny-town",
  "version": "0.1.0",
  "private": true,
  "description": "Sunny Town - game nông trại đa nền tảng (H5, PC, Android, iOS) viết bằng TypeScript + Phaser 3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "electron": "npm run build && electron electron/main.cjs",
    "cap:add:android": "npm run build && npx cap add android && npx cap sync",
    "cap:add:ios": "npm run build && npx cap add ios && npx cap sync",
    "cap:sync": "npm run build && npx cap sync"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "optionalDependencies": {},
  "engines": {
    "node": ">=18"
  }
}

{
  "devDependencies": {
    "webpack": "^5.109.2",
    "webpack-cli": "^6.0.1"
  }
}
