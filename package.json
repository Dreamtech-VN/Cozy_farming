{
  "name": "sunny-town",
  "version": "0.1.0",
  "private": true,
  "description": "Sunny Town - game nông trại đa nền tảng (H5, PC, Android, iOS) viết bằng TypeScript + Phaser 3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:app": "tsc --noEmit && vite build --base=./",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "assets:manifest": "python3 scripts/make_preload_manifest.py",
    "electron": "npm run build:app && electron electron/main.cjs",
    "cap:add:android": "npm run build:app && npx cap add android && npx cap sync",
    "cap:add:ios": "npm run build:app && npx cap add ios && npx cap sync",
    "cap:sync": "npm run build:app && npx cap sync"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "webpack": "^5.109.2",
    "webpack-cli": "^6.0.1"
  },
  "optionalDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
